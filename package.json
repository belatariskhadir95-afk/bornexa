{
  "name": "bornexa-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site statique BORNEXA + build de minification (CSS/JS).",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.23.1"
  }
}
